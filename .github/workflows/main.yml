name: Combined Remote Playlist

on:
  workflow_dispatch:
  schedule:
    - cron: '*/40 * * * *'

jobs:
  update-playlist:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.55.0-noble
    env:
      HOME: /root
      EPG_URL: "https://raw.githubusercontent.com/KipkoechVictor3/MyEPG/main/combined_epg.xml.gz"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Environment
        run: |
          apt-get update
          apt-get install -y python3-venv curl
        shell: bash

      - name: Fetch Channels List
        run: |
          curl -L -o channels.txt "https://tinyurl.com/4ze5nm6p"
          tr -d '\r' < channels.txt > channels_clean.txt
          mv channels_clean.txt channels.txt
        shell: bash

      - name: Process and Merge Remote Playlists
        run: |
          # Prevent script from exiting immediately on a failed curl command
          set +e 

          OUTPUT_FILE="MyStuffCLW.m3u"
          echo "#EXTM3U url-tvg=\"${EPG_URL}\"" > "$OUTPUT_FILE"
          
          echo "--- Starting Download and Merge ---"
          
          if [ -f "channels.txt" ]; then
            while read -r url || [ -n "$url" ]; do
              [[ -z "$url" || "$url" =~ ^# ]] && continue
              
              FRIENDLY_NAME=$(echo "$url" | awk -F'/' '{print $NF}' | cut -d'?' -f1 | sed 's/\.m3u8//g; s/\.m3u//g')
              [ -z "$FRIENDLY_NAME" ] && FRIENDLY_NAME="Remote_Stream"

              temp_file="temp_dl.m3u"
              echo "Downloading: $FRIENDLY_NAME from $url"
              
              # Added -4 to force IPv4 (helps with some DNS setups) and lowered connect-timeout
              curl -L -f -k -s -4 \
                --user-agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
                --connect-timeout 15 \
                --retry 2 \
                "$url" -o "$temp_file"
              
              # Check if the file was actually created and has content
              if [ -s "$temp_file" ]; then
                if grep -qE '^(#EXTM3U|#EXTINF)' "$temp_file"; then
                  echo -e "\n\n######## SOURCE: $FRIENDLY_NAME ########" >> "$OUTPUT_FILE"
                  grep -v '^#EXTM3U' "$temp_file" >> "$OUTPUT_FILE"
                  echo "✅ Merged $FRIENDLY_NAME"
                else
                  echo "⚠️ $FRIENDLY_NAME is not a valid M3U. Skipping."
                fi
              else
                echo "❌ Failed to download $FRIENDLY_NAME (Link might be down or port 8081 is blocked)."
              fi
              rm -f "$temp_file"
            done < "channels.txt"
          fi
          
          # Re-enable exit on error for subsequent steps
          set -e
          exit 0
        shell: bash

      - name: Clean and Finalize Playlist
        run: |
          tr -d '\r' < MyStuffCLW.m3u > final_temp.m3u
          sed -E 's/[[:space:]]+$//' final_temp.m3u | grep -v '^[[:space:]]*$' | tr -s '\n' > MyStuffCLW.m3u
          rm final_temp.m3u
          echo "✅ Final cleanup complete."
        shell: bash

      - name: Upload to Dropbox
        uses: gomes042/gh-actions-dropbox/files/upload@23ce81287e83f01c19c9f60b1849b746db6616b5
        with:
          DROPBOX_APP_KEY: ${{ secrets.DROPBOX_APP_KEY }}
          DROPBOX_APP_SECRET: ${{ secrets.DROPBOX_APP_SECRET }}
          DROPBOX_REFRESH_TOKEN: ${{ secrets.DROPBOX_REFRESH_TOKEN }}
          source_path: MyStuffCLW.m3u
          DEST_PATH: /MyStuff/MyStuffCLW.m3u
